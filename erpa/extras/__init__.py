"""Example analyses that build on the ERPA public API.

These modules are not part of the core package and may require the optional
extras dependencies. Install with ``pip install erpa[extras]``.
"""
